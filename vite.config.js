import { defineConfig } from 'vite'

export default defineConfig({
  // Si tu web se despliega en una subcarpeta (ej: nombre-usuario.github.io/retomusical/)
  // cambia el valor de 'base' a '/retomusical/'
  base: './', 
})
